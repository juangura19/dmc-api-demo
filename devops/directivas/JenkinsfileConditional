pipeline {
    agent any
    parameters {
        booleanParam(name: 'TOOGLE', defaultValue: false, description: 'Yes or Not')
    }
    stages {

        stage('Test') {
            when {
                expression {
                    return params.UPLOAD ==~ /(?i)(Y|YES|T|TRUE|ON|RUN)/
                }
            }
            steps {
                echo "Toggle: ${params.TOGGLE}"

            }
        }
    }
}
