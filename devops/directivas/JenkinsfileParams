pipeline {
    agent any

    stages {
        stage('Test') {
            options {
                retry(4)
            }
            steps {
                sh 'aws configure'
            }
        }
    }
}
