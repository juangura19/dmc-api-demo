pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'echo $BUILD_URL}'
                sh 'echo $JOB_NAME}'
                sh 'echo $BUILD_NUMBER}'
                sh 'echo $BUILD_URL}'
                sh 'echo $WORKSPACE}'
            }
        }
    }
}
