pipeline {
    agent {
        docker {
            image 'amazon/aws-cli:latest'
            args '--entrypoint=bash'
        }
    }
    stages {
        stage ('Test') {
            steps {
                sh 'cat /etc/*release'
                sh 'aws --version'
            }
        }

    }

}
