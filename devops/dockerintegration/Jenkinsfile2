pipeline {
    agent {
        docker {
            image 'amazonlinux:latest'
        }
    }
    stages {
        stage ('Test') {
            steps {
                sh 'aws --version'
            }
        }
    }
}
